name: meddeck
description: Clinical slide library app
publish_to: 'none'

version: 0.1.0

environment:
  sdk: '^3.10.0'

dependencies:
  cloud_firestore: ^5.6.0
  flutter:
    sdk: flutter

  go_router: ^14.2.0
  cached_network_image: ^3.3.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
    
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  google_sign_in: ^6.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

